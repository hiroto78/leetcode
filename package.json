{
  "dependencies": {
    "eslint": "^6.8.0",
    "eslint-config-google": "^0.14.0",
    "eslint-watch": "^6.0.1"
  },
  "scripts": {
    "lint": "eslint --cache ./*.js",
    "watch": "npm-run-all --parallel watch:*",
    "watch:lint": "chokidar * -c 'npm run -s lint'"
  },
  "devDependencies": {
    "chokidar-cli": "^2.1.0",
    "npm-run-all": "^4.1.5"
  }
}
